"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.armaldio_mouse_lock;

	PLUGIN_CLASS.Type = class armaldio_mouse_lockType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
